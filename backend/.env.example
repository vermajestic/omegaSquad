# Optional live connectors
AIS_API_URL=
AIS_API_KEY=
ENVIRONMENT_API_URL=
ENVIRONMENT_API_KEY=
# Optional learned segmentation model. The backend uses the transparent baseline detector if empty.
OIL_SPILL_MODEL_PATH=
